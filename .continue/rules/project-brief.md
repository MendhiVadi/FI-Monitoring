# Forest Watch project rule

Always read the workspace-root `PROJECT_BRIEF.md` before proposing or applying a
change. The canonical workspace is `C:\Users\medha\Desktop\Hackathon Project`,
with application code in `frontend/`. Do not use `Newsletter-Platform` as
context. Preserve existing edits and never write concurrently with another AI.

