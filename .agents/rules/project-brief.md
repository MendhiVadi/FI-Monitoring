# Forest Watch project rule

Before acting, read the workspace-root `PROJECT_BRIEF.md` and follow it as the
single source of truth. Work in `frontend/` and do not use `Newsletter-Platform`
for this project. Preserve existing edits and keep only one active AI writer.

