# Shared AI Instructions

Before doing any work, read `PROJECT_BRIEF.md` in this directory. It defines the
problem, minimum judging criteria, current implementation, missing work, and
shared safety rules.

The canonical workspace is `C:\Users\medha\Desktop\Hackathon Project`; application
code lives in `frontend/`. Never use `Newsletter-Platform` for this project.
Preserve changes from other tools and allow only one AI tool to write at a time.

